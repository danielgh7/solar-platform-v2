import type {ProposalBranding} from '../domain/proposal/types';import type {Brand} from './brand';
export function proposalBrandFromOrganization(brand:Brand,logoDataUrl?:string):ProposalBranding{return{companyDisplayName:brand.displayName,logoDataUrl,primaryColor:brand.primaryColor,accentColor:brand.accentColor,secondaryColor:brand.secondaryColor,footerText:brand.footerText||`${brand.displayName} · ${brand.tagline}`,website:brand.website||'',phone:brand.phone||'',email:brand.email||'',legalDisclaimer:'Los resultados técnicos y financieros son estimaciones de planeación basadas en los supuestos documentados de este proyecto. La producción, los recibos de la compañía suministradora y los resultados financieros reales pueden variar.',coverTagline:brand.tagline||'Energía que conecta.'}}
export async function assetUrlToDataUrl(url:string){const r=await fetch(url,{credentials:'include'});if(!r.ok)throw new Error('logo-fetch-failed');const blob=await r.blob();return await new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result));reader.onerror=()=>reject(reader.error);reader.readAsDataURL(blob)})}
