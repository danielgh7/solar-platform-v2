import type {SizingAssumptions,SizingScenario} from './types';
export function sizeForOffset(annualKwh:number,targetOffset:number,a:SizingAssumptions){const targetEnergy=annualKwh*(targetOffset/100);const effectiveYield=a.specificYield*a.lossFactor;const requiredDcKwp=effectiveYield>0?targetEnergy/effectiveYield:0;const withMargin=requiredDcKwp*(1+a.designMargin/100);const panelKw=a.panelWattage/1000;const panelCount=panelKw>0?Math.ceil(withMargin/panelKw):0;const dcKwp=panelCount*panelKw;const annualProduction=dcKwp*effectiveYield;const achievedOffset=annualKwh>0?Math.min(annualProduction/annualKwh*100,999):0;return{targetOffset,panelCount,requiredDcKwp,dcKwp,annualProduction,achievedOffset,deltaToTarget:achievedOffset-targetOffset};}
export function generateSizingScenarios(annualKwh:number,target:number,a:SizingAssumptions):SizingScenario[]{const targets=[Math.max(50,target-10),target,Math.min(100,Math.max(target+5,100))];const ids=['conservative','recommended','maximum'] as const;const names=['Conservative','Recommended','Maximum coverage'];return ids.map((id,i)=>({id,name:names[i],...sizeForOffset(annualKwh,targets[i],a),assumptions:{...a}}));}
