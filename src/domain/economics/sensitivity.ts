import {buildCashFlows} from './cashflow';
import {calculateFinancialMetrics} from './metrics';
import type {FinancialAssumptions,SensitivityCell,SolarValueModel} from './types';
export function calculateSensitivity(baseInvestment:number,firstYearProduction:number,solar:SolarValueModel,financial:FinancialAssumptions):SensitivityCell[]{const escalations=[Math.max(0,financial.electricityEscalationPct-2),financial.electricityEscalationPct,financial.electricityEscalationPct+2];const capex=[.9,1,1.1];const cells:SensitivityCell[]=[];for(const escalationPct of escalations)for(const capexFactor of capex){const f={...financial,electricityEscalationPct};const investment=baseInvestment*capexFactor;const flows=buildCashFlows(investment,firstYearProduction,solar,f);const m=calculateFinancialMetrics(flows,investment,f.discountRatePct);cells.push({escalationPct,capexFactor,paybackYears:m.simplePaybackYears,npvMxn:m.npvMxn,irrPct:m.irrPct});}return cells;}
