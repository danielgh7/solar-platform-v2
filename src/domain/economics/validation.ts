import type {BaselineElectricityCost,EconomicReadiness,EconomicValidationIssue,FinancialAssumptions,ProjectPricing,SolarValueAssumptions} from './types';
import type {ElectricalValidationIssue} from '../electrical/types';
export function validateEconomics(args:{baseline:BaselineElectricityCost;pricing:ProjectPricing;vatRatePct:number;financial:FinancialAssumptions;solar:SolarValueAssumptions;marginWarningThresholdPct:number;annualProductionKwh:number;electricalIssues:ElectricalValidationIssue[]}):EconomicValidationIssue[]{const a=args,issues:EconomicValidationIssue[]=[];if(a.baseline.mode==='historical'&&!a.baseline.valid)issues.push({id:'historical-invalid',severity:'error',code:'historical-bill-invalid',message:'Historical baseline requires 12 valid billed amounts and positive annual consumption.'});if(a.baseline.blendedRateMxnPerKwh<=0)issues.push({id:'tariff-invalid',severity:'error',code:'blended-rate-invalid',message:'Blended electricity rate must be greater than zero.'});if(a.annualProductionKwh<=0)issues.push({id:'production-missing',severity:'error',code:'production-missing',message:'R3 production is missing or zero.'});if(a.pricing.sellPriceBeforeVat<=0)issues.push({id:'sell-invalid',severity:'error',code:'sell-price-invalid',message:'Project sell price must be greater than zero.'});if(a.vatRatePct<0||a.vatRatePct>100)issues.push({id:'vat-invalid',severity:'error',code:'vat-invalid',message:'VAT rate must be between 0% and 100%.'});if(a.financial.discountRatePct<0||a.financial.discountRatePct>100)issues.push({id:'discount-invalid',severity:'error',code:'discount-invalid',message:'Discount rate must be between 0% and 100%.'});if(a.financial.electricityEscalationPct<0||a.financial.electricityEscalationPct>30)issues.push({id:'escalation-invalid',severity:'error',code:'escalation-invalid',message:'Electricity escalation must be between 0% and 30%.'});if(a.financial.pvDegradationPct<0||a.financial.pvDegradationPct>10)issues.push({id:'degradation-invalid',severity:'error',code:'degradation-invalid',message:'PV degradation must be between 0% and 10%.'});if(a.financial.omEscalationPct<0||a.financial.omEscalationPct>30)issues.push({id:'om-escalation-invalid',severity:'error',code:'om-escalation-invalid',message:'O&M escalation must be between 0% and 30%.'});if(a.financial.analysisTermYears<1||a.financial.analysisTermYears>50)issues.push({id:'term-invalid',severity:'error',code:'term-invalid',message:'Analysis term must be between 1 and 50 years.'});if(a.pricing.grossMarginPct<a.marginWarningThresholdPct)issues.push({id:'margin-low',severity:'warning',code:'margin-low',message:`Gross margin ${a.pricing.grossMarginPct.toFixed(1)}% is below the configured ${a.marginWarningThresholdPct.toFixed(1)}% warning threshold.`});if(a.solar.exportMode==='same-rate')issues.push({id:'export-aggressive',severity:'warning',code:'export-aggressive',message:'Surplus valued at the same blended retail rate is an aggressive simplified assumption.'});if(a.electricalIssues.some(i=>i.severity==='error'))issues.push({id:'r3-blocking',severity:'error',code:'r3-electrical-blocking',message:'R3 electrical design has blocking errors; economics are not ready.'});return issues;}
export function economicReadiness(issues:EconomicValidationIssue[],irrUnavailable=false):EconomicReadiness{if(issues.some(i=>i.severity==='error'))return'Blocked';if(irrUnavailable||issues.some(i=>i.severity==='warning'))return'Needs review';return'Ready';}
