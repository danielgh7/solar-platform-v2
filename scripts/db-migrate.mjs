import pg from 'pg';import fs from 'node:fs/promises';import path from 'node:path';
const url=process.env.DATABASE_URL;if(!url)throw new Error('DATABASE_URL is required');const pool=new pg.Pool({connectionString:url});try{await pool.query('create table if not exists schema_migrations(version text primary key,applied_at timestamptz not null default now())');const dir=path.resolve('db/migrations');const files=(await fs.readdir(dir)).filter(x=>x.endsWith('.sql')).sort();for(const file of files){const version=file.replace(/\.sql$/,'');const done=await pool.query('select 1 from schema_migrations where version=$1',[version]);if(done.rowCount){console.log(`skip ${version}`);continue}const sql=await fs.readFile(path.join(dir,file),'utf8');await pool.query(sql);console.log(`applied ${version}`)}console.log('database migrations complete')}finally{await pool.end()}
