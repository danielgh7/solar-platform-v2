import{createCanvas,loadImage}from'@napi-rs/canvas';import{readFile,writeFile,mkdir}from'node:fs/promises';
const src='src/assets/branding/buenos-dias-sol-by-enertika.png',out='src-tauri/icons';await mkdir(out,{recursive:true});const img=await loadImage(await readFile(src));for(const n of[32,128,256,512]){const c=createCanvas(n,n),x=c.getContext('2d');x.fillStyle='#fff';x.fillRect(0,0,n,n);const pad=n*.08,w=n-pad*2,h=w*img.height/img.width;x.drawImage(img,pad,(n-h)/2,w,h);await writeFile(`${out}/${n}x${n}.png`,c.toBuffer('image/png'))}await writeFile(out+'/icon.png',await readFile(out+'/512x512.png'));
