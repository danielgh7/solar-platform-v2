import{chromium}from'playwright';import{mkdir,writeFile}from'node:fs/promises';import{createHash}from'node:crypto';
const base=process.env.R71_BASE_URL||'http://127.0.0.1:4173',out='artifacts/r7.1';await mkdir(out,{recursive:true});const browser=await chromium.launch({headless:true});
const shots=[['web-desktop.png',1440,1000,'/login'],['web-mobile.png',390,844,'/login'],['android-phone.png',412,915,'/login'],['android-tablet.png',800,1280,'/login'],['ios-iphone.png',393,852,'/login'],['ios-ipad.png',1024,1366,'/login'],['windows-app.png',1440,900,'/login'],['macos-app.png',1440,900,'/login'],['login-native.png',393,852,'/login'],['crm-mobile.png',390,844,'/crm'],['project-mobile.png',390,844,'/projects'],['proposal-mobile.png',390,844,'/projects/SOL-2026-0184/proposal']];
for(const[n,w,h,path]of shots){const p=await browser.newPage({viewport:{width:w,height:h}});await p.goto(base+path,{waitUntil:'networkidle'});await p.screenshot({path:out+'/'+n,fullPage:true});await p.close()}await browser.close();
const manifest={schema:1,generatedAt:new Date().toISOString(),sourceRevision:process.env.GITHUB_SHA||'local',note:'Browser viewport evidence. Native build/runtime evidence is collected separately by platform CI jobs.',files:shots.map(([name,w,h])=>({name,width:w,height:h,kind:name.startsWith('web-')?'web-browser':'representative-viewport'}))};await writeFile(out+'/evidence-manifest.json',JSON.stringify(manifest,null,2));
