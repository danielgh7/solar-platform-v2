import {chromium} from 'playwright';import {seedR6,PROJECT} from './r6-fixture.mjs';
const base=process.env.R6_BASE_URL||'http://127.0.0.1:4173',assert=(v,m)=>{if(!v)throw new Error(m)};const browser=await chromium.launch({headless:true}),context=await browser.newContext({viewport:{width:1600,height:1000},acceptDownloads:true}),page=await context.newPage();await seedR6(page,base);await page.goto(`${base}/crm`,{waitUntil:'networkidle'});await page.waitForSelector('[data-testid="crm-opportunities-table"]');assert((await page.locator('.crm-table tbody tr').count())>=3,'CRM opportunity table not populated');
await page.getByRole('button',{name:/New solar opportunity/}).click();await page.waitForSelector('[data-testid="crm-opportunity-detail"]');assert((await page.getByLabel('Account name').inputValue()).startsWith('New solar lead'),'create opportunity failed');await page.getByLabel('Account name').fill('Browser Validation Solar Lead');await page.getByLabel('Contact name').fill('Validation Contact');await page.getByLabel('Contact email').fill('validation@example.test');await page.goto(`${base}/crm`,{waitUntil:'networkidle'});assert((await page.locator('.crm-table').innerText()).includes('Browser Validation Solar Lead'),'account/contact edit persistence missing');
await page.getByRole('button',{name:'Board'}).click();assert(await page.locator('[data-testid="crm-pipeline-board"]').isVisible(),'pipeline board missing');const leadCard=page.locator('.pipeline-card').filter({hasText:'Browser Validation Solar Lead'});await leadCard.getByLabel(/Move .* stage/).selectOption('proposal-sent');assert((await page.locator('.r6-message').innerText()).includes('Proposal Sent requires'),'invalid stage movement not blocked');
await page.goto(`${base}/crm/opportunities/opp-montebello`,{waitUntil:'networkidle'});assert((await page.locator('[data-testid="crm-opportunity-detail"]').innerText()).includes(PROJECT),'linked solar project missing');assert((await page.locator('[data-testid="crm-proposal-followup"]').innerText()).includes('Exported'),'R5 proposal follow-up missing');await page.getByLabel('Opportunity stage').selectOption('negotiation');assert((await page.locator('[data-testid="crm-opportunity-detail"]').innerText()).includes('Negotiation'),'valid stage movement failed');
await page.getByLabel('Activity note').fill('Validated commercial call with customer.');await page.getByRole('button',{name:'Add activity'}).click();assert((await page.locator('[data-testid="crm-activity-timeline"]').innerText()).includes('Validated commercial call'),'activity creation failed');await page.getByLabel('Task title').fill('Validation follow-up task');await page.getByLabel('Task due date').fill('2026-09-08');await page.getByLabel('Task priority').selectOption('urgent');await page.getByRole('button',{name:/Add task/}).click();assert((await page.locator('[data-testid="crm-tasks"]').innerText()).includes('OVERDUE'),'overdue task state missing');await page.getByRole('button',{name:'Complete Validation follow-up task'}).click();assert((await page.locator('[data-testid="crm-activity-timeline"]').innerText()).includes('Completed task: Validation follow-up task'),'task completion timeline missing');
await page.getByLabel('Loss reason').selectOption('loss-1');await page.getByRole('button',{name:/Mark lost/}).click();assert((await page.locator('.detail-status-strip').innerText()).includes('Lost'),'mark lost failed');await page.getByRole('button',{name:/Reopen opportunity/}).click();assert((await page.locator('.detail-status-strip').innerText()).includes('Negotiation'),'reopen failed');await page.getByRole('button',{name:/Mark won/}).click();assert((await page.locator('[data-testid="crm-handoff-checklist"]').innerText()).includes('Sold / handoff pending'),'won did not create handoff pending');assert(!(await page.locator('[data-testid="crm-handoff-checklist"]').innerText()).includes('Installation planning'),'won incorrectly auto-advanced installation readiness');
const checklist=page.locator('[data-testid="crm-handoff-checklist"]');for(const label of ['Customer/contact data complete','Site information complete','Payment/contract status manually recorded','Interconnection/document requirements manually reviewed']){const sel=checklist.getByLabel(`Handoff ${label}`);await sel.selectOption('complete');}await checklist.getByLabel('Delivery state').selectOption('Commercial handoff complete');await checklist.getByLabel('Delivery state').selectOption('Technical handoff ready');assert((await checklist.innerText()).includes('Technical handoff ready'),'handoff transition after required checklist failed');
await page.evaluate(()=>{const k='solar-platform-v2:r1:energy-profile',p=JSON.parse(localStorage.getItem(k));p.consumption=p.consumption.slice(0,2);p.updatedAt=new Date().toISOString();localStorage.setItem(k,JSON.stringify(p));});await page.reload({waitUntil:'networkidle'});assert((await page.locator('[data-testid="crm-blockers"]').innerText()).includes('r1-consumption'),'inherited R1 blocker not propagated');await page.goto(`${base}/projects/${PROJECT}`,{waitUntil:'networkidle'});assert(await page.locator('[data-testid="workspace-r6"]').isVisible(),'Workspace R6 strip missing');await page.goto(`${base}/crm`,{waitUntil:'networkidle'});await page.getByRole('button',{name:'Analytics'}).click();assert(await page.locator('[data-testid="crm-funnel"]').isVisible(),'analytics/funnel missing');await page.reload({waitUntil:'networkidle'});assert(await page.locator('[data-testid="crm-funnel"]').isVisible(),'CRM view preference persistence failed');await browser.close();console.log('R6 browser validation passed: create/open, account/contact, project link, table, board, valid/blocked stage movement, timeline, tasks/overdue/completion, R5 follow-up, lost/reopen/won, explicit handoff, inherited blocker, Workspace, persistence and analytics/funnel.');
